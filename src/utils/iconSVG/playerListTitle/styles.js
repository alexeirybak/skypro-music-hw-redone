import styled from 'styled-components';

export const PlayerListTitleImg = styled.svg`
  circle {
    fill: transparent;
    stroke: var(--play-list-title);
  }
  path {
    fill: transparent;
    stroke: var(--play-list-title);
  }
`;
